<Window x:Class="Aplicacion3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Aplicacion3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--<Grid>
        <Button Height="200" Width="400" Content="Click!!" Background="Yellow">
            <Button.Style>
                <Style TargetType="Button">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="FontSize" Value="32"></Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>-->

    <StackPanel>
        <TextBox Width="100" Name="Cuadro" Margin="150" Text="{Binding ElementName=Slider,
            Path=Value, Mode=TwoWay}"></TextBox>
        <Slider Width="350" Name="Slider" Minimum="0" Maximum="100"></Slider>
    </StackPanel>
    
    
</Window>
